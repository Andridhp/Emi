import{describe,expect,it}from'vitest';import{comfortDaySummary,comfortHistory}from'../src/lib/comfortHistory';const event=(id:string,title:string,profileId='p',occurredAt='2026-07-25T10:00:00')=>({id,profileId,kind:'comfort' as const,title,detail:'',occurredAt,source:'parent' as const,data:{context:'Después de comer'}});describe('Rutinas y confort',()=>{it('distingue episodios y rutinas desde la categoría explícita',()=>{expect(comfortHistory([event('1','Irritabilidad'),event('2','Gases'),event('3','Regurgitación'),event('4','Rutina')],'p').map((item)=>item.type).sort()).toEqual(['gas','irritability','regurgitation','routine'])});it('conserva contexto sin inferir causalidad',()=>{expect(comfortHistory([event('1','Gases')],'p')[0].context).toBe('Después de comer')});it('resume el día y aísla el perfil',()=>{const records=comfortHistory([event('1','Gases'),event('2','Rutina'),event('3','Irritabilidad','otro')],'p');expect(comfortDaySummary(records,new Date('2026-07-25T12:00:00'))).toEqual({records:2,episodes:1,routines:1})});});
